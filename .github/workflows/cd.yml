name: ⬣ Deploy

on:
  workflow_run:
    workflows: ["Typecheck", "Lint", "Format"]
    types:
      - completed

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v4

      - name: 🔨 Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 8

      - name: ⎔ Setup node
        uses: actions/setup-node@v4
        with:
          cache: pnpm
          cache-dependency-path: ./package.json
          node-version: 18

      - name: 📥 Install deps
        run: pnpm install

      - name: Deploy
        run: |
          echo "Deploying..."
          echo "Deployed!"
